---
- name: Harden Proxmox
  hosts: proxmox
  gather_facts: true 
  become: true  
  vars:
    admin_user: administrator
    ssh_public_key: "{{ lookup('file', '~/.ssh/ansible_proxmox.pub') }}"
    management_cidr: "10.0.0.0/24"

  tasks:
    # 1. SSH — create administrator user and disable root login
    - name: Create administrator user
      ansible.builtin.user:
        name: "{{ admin_user }}"
        shell: /bin/bash
        groups: sudo
        append: true
        state: present
      when: ansible_user == 'root' 

    - name: Add SSH public key for administrator
      ansible.posix.authorized_key:
        user: "{{ admin_user }}"
        key: "{{ ssh_public_key }}"
        state: present
      when: ansible_user == 'root' 

    - name: Install sudo
      ansible.builtin.apt:
        name: sudo
        state: present
        update_cache: false   

    - name: Grant passwordless sudo
      ansible.builtin.copy:
        dest: /etc/sudoers.d/administrator
        content: "administrator ALL=(ALL) NOPASSWD:ALL\n"
        owner: root
        group: root
        mode: "0440"
        validate: /usr/sbin/visudo -cf %s
      when: ansible_user == 'root' 

    - name: Disable root SSH login
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PermitRootLogin'
        line: 'PermitRootLogin no'
        state: present
      when: ansible_user == 'root' 

    - name: Disable password authentication
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PasswordAuthentication'
        line: 'PasswordAuthentication no'
        state: present

    - name: Enable public key authentication
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PubkeyAuthentication'
        line: 'PubkeyAuthentication yes'
        state: present

    - name: Restart SSH
      ansible.builtin.service:
        name: ssh
        state: restarted

    # 2. Configure Proxmox Firewall — datacenter and node levels
    - name: Write datacenter firewall config (firewall disabled until rules are in place)    
      ansible.builtin.shell:
        cmd: |
          cat > /etc/pve/firewall/cluster.fw << 'FWEOF'
          [OPTIONS]
          enable: 0
          policy_in: DROP
          policy_out: ACCEPT

          [RULES]
          IN SSH(ACCEPT) -source {{ management_cidr }} -log nolog
          IN ACCEPT -p tcp -dport 8006 -source {{ management_cidr }} -log nolog
          IN ACCEPT -p tcp -dport 111 -log nolog
          IN ACCEPT -p udp -dport 111 -log nolog
          IN ACCEPT -p tcp -dport 2049 -log nolog
          FWEOF
      changed_when: true

    - name: Ensure node firewall directory exists
      ansible.builtin.shell: mkdir /etc/pve/nodes/{{ ansible_hostname }}
      args:
        creates: /etc/pve/nodes/{{ ansible_hostname }}

    - name: Write node-level firewall config (disabled — inherits datacenter rules)
      ansible.builtin.shell:
        cmd:  |
          cat > /etc/pve/nodes/{{ ansible_hostname }}/host.fw << 'FWEOF'
          [OPTIONS]
          enable: 0
          FWEOF
      changed_when: true

    - name: Enable datacenter firewall
      ansible.builtin.shell:
        cmd: >-
          python3 -c "p='/etc/pve/firewall/cluster.fw';
          open(p,'w').write(open(p).read().replace('enable: 0','enable: 1',1))"
      changed_when: true

    - name: Enable node firewall
      ansible.builtin.shell:
        cmd: >-
          python3 -c "p='/etc/pve/nodes/{{ ansible_hostname }}/host.fw';
          open(p,'w').write(open(p).read().replace('enable: 0','enable: 1',1))"
      changed_when: true

<<<<<<< HEAD
=======
    # Generate SSL Certificate using IONOS DNS challenge
    - name: Ensure pexpect is installed
      ansible.builtin.apt:
        name: python3-pexpect
        state: present

    - name: Register ACME account
      ansible.builtin.expect:
        command: >-
          pvenode acme account register default {{ acme_email }}
          --directory https://acme-v02.api.letsencrypt.org/directory
        responses:
          '(?i)agree': 'y'
        timeout: 30
      register: acme_account
      changed_when: acme_account.rc == 0
      failed_when: acme_account.rc != 0 and 'already exists' not in acme_account.stdout
  
    - name: Write DNS credentials to temp file
      ansible.builtin.copy:
        content: "{{ acme_dns_credentials }}"
        dest: /tmp/acme_dns_creds
        mode: "0600"
      no_log: true

    - name: Add IONOS DNS plugin
      ansible.builtin.command:  
        argv:
          - pvenode
          - acme
          - plugin
          - add
          - dns
          - "{{ acme_dns_api }}"
          - --api
          - "{{ acme_dns_api }}"
          - --data
          - "/tmp/acme_dns_creds"
      register: plugin_result
      changed_when: plugin_result.rc == 0
      failed_when: plugin_result.rc != 0 and 'already exists' not in plugin_result.stderr

    - name: Configure ACME domain on node
      ansible.builtin.command:
        cmd: >-
          pvenode config set
          --acmedomain0 "domain={{ acme_domain }},plugin={{ acme_dns_plugin_name }}"
      changed_when: true

    - name: Order SSL certificate
      ansible.builtin.command:
        cmd: pvenode acme cert order --force
      changed_when: true

>>>>>>> 2031198e734698507c5b224b68c65cb1dadda611
    #  Remove enterprise subscriptions, update and reboot
    - name: Disable PVE enterprise repo
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/pve-enterprise.list
        content: "# deb https://enterprise.proxmox.com/debian/pve {{ ansible_distribution_release }} pve-enterprise\n"
        owner: root
        group: root
        mode: "0644"

    - name: Disable Ceph enterprise repo
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/ceph.list
        content: "# deb https://enterprise.proxmox.com/debian/ceph-squid {{ ansible_distribution_release }} enterprise\n"
        owner: root
        group: root
        mode: "0644"

    - name: Enable no-subscription repo
      ansible.builtin.copy:
        dest: /etc/apt/sources.list.d/pve-no-subscription.list
        content: "deb http://download.proxmox.com/debian/pve {{ ansible_distribution_release }} pve-no-subscription\n"
        owner: root
        group: root
        mode: "0644"

    - name: Run apt update and dist-upgrade
      ansible.builtin.apt:
        update_cache: true
        upgrade: dist

    - name: Check if reboot is required
      ansible.builtin.stat:
        path: /var/run/reboot-required
      register: reboot_required

    - name: Reboot if kernel updated
      ansible.builtin.reboot:
        reboot_timeout: 120
<<<<<<< HEAD
      when: reboot_required.stat.exists
=======
      when: reboot_required.stat.exists
>>>>>>> 2031198e734698507c5b224b68c65cb1dadda611
